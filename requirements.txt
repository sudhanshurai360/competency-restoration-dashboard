# Reproduces the verify gate (src/verify.py) on the environment that produced this release.
pandas==2.3.3
pdfplumber==0.11.10
pyarrow==24.0.0
# Python 3.13
pytesseract==0.3.13  # src/co_special_master.py's _ocr_recover_sm_2024_11_28 ONLY -- sm_2024-11-28.pdf
# embeds a broken/cid-encoded font on 2 of its pages that pdfplumber's normal text layer can't read.
# Also requires the tesseract SYSTEM binary (e.g. `brew install tesseract` on macOS, `apt install
# tesseract-ocr` on Debian/Ubuntu) -- not installable via pip alone. No other extractor needs this.
